# 声明式 vs 命令式：IR 设计的核心权衡

## 你的洞察展开

```
MLIR (声明式)              Triton (命令式)
"要什么"         →         "怎么做"
用户端简单       →         编译器端复杂
编译器端复杂     →         用户端需要专业知识

┌─────────────────────────────────────┐
│ 这决定了整个 IR 的设计方向！        │
└─────────────────────────────────────┘
```

---

## 从 Tiling 看这个权衡

### 场景：用户想要一个高效的矩阵乘法

#### 方式1：MLIR（声明式）

```
用户的代码：
┌────────────────────────────┐
│ %C = linalg.matmul         │
│      ins(%A, %B)           │
│      outs(%C)              │
│                            │
│ "我要矩阵乘法"             │
└────────────────────────────┘
          ↓
编译器决策（自动）：
┌────────────────────────────┐
│ 1. 选择 tile size = 32     │
│ 2. 生成 3 层循环嵌套       │
│ 3. 生成 subview            │
│ 4. 应用融合优化            │
│ 5. 应用向量化              │
└────────────────────────────┘
          ↓
生成高效代码
```

**用户努力**：✅ 最小（1 行代码）
**编译器复杂度**：❌ 高（需要启发式算法）
**调优难度**：❌ 困难（如果结果不好，用户无法控制）

---

#### 方式2：Triton（命令式）

```
用户的代码：
┌──────────────────────────┐
│ @triton.jit              │
│ def matmul(A, B, C, ..., │
│    BLOCK_M, BLOCK_N,     │
│    BLOCK_K):             │
│   ...                    │
│   tl.load(A)             │
│   tl.dot(...)            │
│   tl.store(C)            │
│                          │
│ "在这个位置，用这个      │
│  block大小做这个计算"    │
└──────────────────────────┘
          ↓
编译器执行（用户指定）：
┌──────────────────────────┐
│ 1. 内存合并（自动）      │
│ 2. 共享内存管理（自动）  │
│ 3. 线程分配（自动）      │
│ 4. warp 优化（自动）     │
└──────────────────────────┘
          ↓
生成高效代码
```

**用户努力**：❌ 中等（需要理解GPU模型）
**编译器复杂度**：✅ 低（主要是代码生成）
**调优难度**：✅ 简单（改 BLOCK_M/N/K 即可）

---

## 权衡对 IR 设计的影响

### 权衡表

| 维度             | 声明式 IR（MLIR）     | 命令式 IR（Triton） |
| ---------------- | --------------------- | ------------------- |
| **操作粒度**     | 高层 + 低层混合       | 单一 Block 级       |
| **操作数量**     | 多（支持多层优化）    | 少（专注计算）      |
| **元数据重要性** | ⭐⭐⭐ 关键（Pass 决策） | ⭐ 轻（用户决策）    |
| **控制流复杂度** | 中（多个层级）        | 简（单一层级）      |
| **优化空间**     | 大（多个 Pass）       | 小（预定义优化）    |
| **错误容易度**   | 低（用户难犯错）      | 高（用户易犯错）    |

---

## 从 Tiling 看 IR 设计的 3 个关键问题

### 问题1：应该保留多少个操作抽象层级？

#### MLIR 的答案：**多层**

```
Layer 1（用户/应用）:
  linalg.matmul          ← "我要矩阵乘法"
  属性：indexing_maps, iterator_types
  
   ↓ Tiling Pass
   
Layer 2（循环级）:
  scf.for
  memref.subview
  func.call
  属性：无
  
   ↓ 后续优化 Pass
   
Layer 3（优化友好）:
  affine.for
  affine.max / affine.min
  vector.transfer
  
   ↓ 代码生成
   
Layer 4（目标）:
  LLVM IR
```

**Why 多层**：
- Tiling 在 Layer 1 → Layer 2
- 循环融合在 Layer 2 → Layer 3
- 向量化在 Layer 3 → Layer 4
- 每一层都有特定的优化

---

#### Triton 的答案：**单层**

```
Layer 1（用户/应用）:
  @triton.jit
  tl.load, tl.dot, tl.store
  属性：BLOCK_M, BLOCK_N, BLOCK_K
  
   ↓ Triton 编译器
   
Layer 2（目标）:
  LLVM IR → PTX
```

**Why 单层**：
- Triton 把所有优化融合在编译器中
- 用户指定计算的"what"，不关心"how"
- 编译器一次性处理所有优化

---

### 问题2：谁来决定分块大小？

#### MLIR：**编译器启发式算法**

```cpp
// Tiling Pass 内的决策逻辑
if (targetIsGPU()) {
  // L2 缓存大小通常是 256KB
  // float32 = 4 bytes
  // 256KB / 4 / 64 (B tile width) = 16 行 (A tile)
  tileSizes = [16, 64, 64];
} else if (targetIsCPU()) {
  // L1 缓存 32KB
  tileSizes = [32, 32, 16];
}
```

**优点**：用户无需知道硬件细节
**缺点**：启发式可能不是最优

---

#### Triton：**用户手动指定**

```python
# 用户需要理解 GPU 的 SM、warp、tensor core
@triton.jit
def kernel(..., BLOCK_M: tl.constexpr = 64,
                BLOCK_N: tl.constexpr = 64,
                BLOCK_K: tl.constexpr = 32):
    ...

# 用户根据 GPU 特性调整
# RTX 3080: 更新器，支持更大的 block
# A100: Tensor Core，需要特定 block 大小
```

**优点**：用户可以精细调优
**缺点**：需要 GPU 编程专业知识

---

### 问题3：错误如何处理？

#### MLIR：**编译器负责正确性**

```
用户写：
  linalg.matmul
  ← 一定是正确的语义
  
编译器保证：
  无论生成什么代码，都计算相同的结果
  
即使性能不好，也不会有错误结果
```

**含义**：IR 操作必须有**明确的语义定义**

---

#### Triton：**用户负责正确性**

```
用户写：
  tl.load(ptr)              ← 用户确保指针正确
  for k in range(0, K, ...):
    ...                     ← 用户确保循环逻辑正确
  tl.store(...)             ← 用户确保存储位置正确
  
编译器假设：
  用户写的计算是对的，我来优化
  
如果用户算法错误，结果就错误
```

**含义**：IR 操作更多是**执行语义**而非**声明语义**

---

## 为 Tiling 设计 IR 的决策树

```
┌─ 你希望用户的努力最小化吗？
│  │
│  ├─ 是（用户只说要什么）
│  │  └─ 选择声明式（MLIR 方向）
│  │     ├─ 需要多层操作
│  │     ├─ 需要丰富元数据
│  │     ├─ 需要启发式算法
│  │     └─ Tiling Pass 很复杂
│  │
│  └─ 否（用户愿意指定细节）
│     └─ 选择命令式（Triton 方向）
│        ├─ 需要单一清晰的层级
│        ├─ 需要清晰的执行模型
│        ├─ 用户参与优化决策
│        └─ Tiling 逻辑在用户代码中

┌─ 你需要跨多个平台吗？
│  │
│  ├─ 是（CPU, GPU, TPU 等）
│  │  └─ 声明式更好
│  │     （同一份代码，多个 backend）
│  │
│  └─ 否（单一平台，如 NVIDIA GPU）
│     └─ 命令式可行
│        （针对特定平台优化）

┌─ 你想要自动分块大小选择吗？
│  │
│  ├─ 是
│  │  └─ 需要声明式 + 启发式算法
│  │     Tiling Pass 包含决策逻辑
│  │
│  └─ 否
│     └─ 命令式（用户指定）
│        或声明式 + 用户注解 (tile_size)
```

---

## 具体建议：设计支持 Tiling 的 IR

### 如果采用声明式（MLIR 风格）

**必需的设计元素**：

```
1. 多层操作（至少 3 层）
   ├─ Layer 1: 高层操作（linalg.matmul）+ 元数据
   ├─ Layer 2: 循环/内存操作（scf.for, memref.subview）
   └─ Layer 3: 优化友好操作（affine.for）

2. 元数据系统（Pass 用这个做决策）
   ├─ indexing_maps（数据如何访问）
   ├─ iterator_types（循环的性质）
   ├─ tile_hint（可选，用户可以给 Tiling Pass 建议）
   └─ other_metadata

3. Tiling Pass 的启发式算法
   ├─ 硬件检测（是 CPU 还是 GPU？）
   ├─ 缓存大小分析
   ├─ 数据类型分析
   └─ 代价模型

4. 清晰的操作语义
   ├─ 每个操作必须有形式化定义
   └─ 保证 Pass 能正确变换
```

---

### 如果采用命令式（Triton 风格）

**必需的设计元素**：

```
1. 清晰的 Block 编程模型
   ├─ program_id（哪个 block）
   ├─ block_idx（block 内的位置）
   └─ block_size（常数元参数）

2. 最小的操作集
   ├─ load / store（内存访问）
   ├─ 算术操作（只针对 Block）
   ├─ 高级操作（如 dot，对整个 Block）
   └─ 同步原语

3. 执行模型清晰
   ├─ Grid 和 Block 的关系
   ├─ Block 内的并行性（implicit）
   └─ 内存模型（全局内存 vs 共享内存）

4. 编译器优化是预定义的
   ├─ 内存合并（分析 load/store）
   ├─ 共享内存分配（分析数据生命期）
   ├─ warp 优化（自动）
   └─ 不需要启发式，是确定性的
```

---

## MLIR Tiling 为什么这么复杂？

### 核心原因：声明式的代价

```
linalg.matmul 说的是"要什么"
        ↓
Tiling Pass 需要推导"怎么做"

推导过程：
1. 理解操作语义
   └─ 从 indexing_maps 推导循环结构

2. 选择分块大小
   └─ 考虑 L1/L2/L3 缓存、向量宽度等

3. 生成循环嵌套
   └─ 几何变换（维度分割）

4. 处理边界
   └─ 非对齐分块的 min/max 操作

5. 推导新的索引映射
   └─ tile 后的数据访问如何变化

这是一个**逆向工程问题**！
```

**vs Triton**:

```
@triton.jit def kernel(..., BLOCK_M, BLOCK_N, BLOCK_K):
    ...
    
用户已经指定了分块：
  ✓ 分块大小（BLOCK_M/N/K）
  ✓ Block 的计算（显式循环）
  ✓ 内存访问（tl.load/tl.store）
  
编译器只需要：
  ✓ 转换成 LLVM
  ✓ 进行基本的代码生成优化
  ✓ 完成！
```

---

## 总结：设计原则

### 原则1：权衡要明确

```
选择声明式 ←→ 编译器复杂，用户简单
选择命令式 ←→ 编译器简单，用户复杂

不能两者兼得！
```

---

### 原则2：对应的操作集不同

```
声明式 → 需要多层操作
  高层操作（含语义）+ 低层操作（执行）
  
命令式 → 需要清晰的执行模型
  单一清晰的层级，操作都是"执行"语义
```

---

### 原则3：错误处理方式不同

```
声明式 → 操作有形式化语义
  编译器可以验证变换的正确性
  
命令式 → 用户负责正确性
  编译器不做验证，只做代码生成
```

---

## 最终建议

### 如果你在设计 IR 用于支持 Tiling：

**第一步**：决定是声明式还是命令式

```
声明式的好处：
  ✅ 用户代码简洁
  ✅ 自动跨平台
  ✅ 后续优化空间大
  
  ❌ 编译器复杂（Tiling Pass ~883行代码）
  ❌ 调试困难（需要查看中间 IR）
  ❌ 启发式可能不优
```

```
命令式的好处：
  ✅ 编译器简单
  ✅ 用户可控制性强
  ✅ 调试相对容易
  
  ❌ 用户需要专业知识
  ❌ 难以跨平台
  ❌ 用户代码重复多
```

**第二步**：根据选择设计操作集

```
如果选择声明式：
  设计: 高层操作 + 元数据 + 多层低层操作
  重点: 保留语义信息，让 Pass 有决策空间
  
如果选择命令式：
  设计: 清晰的执行模型 + 最小操作集
  重点: 明确的编程模型，不需要推导
```

**第三步**：实现对应的 Pass/编译器

```
如果声明式：
  需要复杂的 Tiling Pass
  需要启发式算法
  需要多个后续 Pass 组合
  
如果命令式：
  需要清晰的代码生成
  需要预定义的优化（内存合并等）
  不需要复杂的决策逻辑
```

